C# Winforms, Entity Framework, SQL Server
Hướng dẫn cài đặt:
- Chạy file .sql trên SQL Server trước
- Sửa lại chuỗi ConnectionString ở file App.config và EFConnectionString ở file LoginForm.cs sao cho phù hợp với tên Server + Database của bạn.
